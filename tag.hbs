{{!< default}}

<div>
<main>

    {{#tag}}
    <div>
        <h1>{{name}}</h1>
        {{#if description}}
            <p>{{description}}</p>
        {{/if}}
    </div>
    {{/tag}}

    <div>
        {{#foreach posts}}
            {{> "loop"}}
        {{/foreach}}
    </div>

</main>
</div>